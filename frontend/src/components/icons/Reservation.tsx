import reservation from "../../assets/icons/Group 2560.png"; // or .png

const Reservation = () => {
  return (
    <img
      src={reservation}
      alt="User Icon"
      width={20}
      height={20}
      className="mr-4 opacity-90"
    />
  );
};

export default Reservation;
